# iOS 性能优化题库与详细答案

> 基于《06-性能优化.pptx》整理  
> 主题：内存优化、CPU/GPU、列表卡顿、离屏渲染、耗电优化、启动优化、安装包瘦身、LinkMap 分析

---

## 目录

1. 性能优化整体思路
2. 内存优化
3. CPU 与 GPU
4. 屏幕成像原理
5. 卡顿产生原因
6. 列表卡顿优化
7. 离屏渲染
8. 卡顿检测
9. 耗电优化
10. App 启动优化
11. 安装包瘦身
12. LinkMap
13. 项目实战回答模板
14. 高频追问汇总

---

# 一、性能优化整体思路

## 题目 1：你在项目中一般从哪些方面做性能优化？

### 答案

iOS 性能优化通常可以从以下几个方向入手：

1. **内存优化**
   - 减少不必要对象创建。
   - 避免循环引用。
   - 图片按需加载、按尺寸加载。
   - 大对象及时释放。
   - 控制缓存大小。

2. **CPU 优化**
   - 减少主线程耗时操作。
   - 布局计算、文本计算、图片解码尽量放到子线程。
   - 避免频繁创建和销毁对象。
   - 减少复杂计算和重复计算。
   - 控制线程并发数量。

3. **GPU 优化**
   - 减少视图层级。
   - 减少透明图层。
   - 避免离屏渲染。
   - 避免短时间显示大量图片。
   - 图片尺寸尽量和显示尺寸一致。

4. **卡顿优化**
   - 保证主线程流畅。
   - 避免在主线程进行 I/O、网络、图片解码、大量 JSON 解析。
   - 使用 Instruments、RunLoop 监控等方式定位卡顿。

5. **耗电优化**
   - 降低 CPU/GPU 使用。
   - 减少定时器。
   - 优化网络请求。
   - 合理使用定位。
   - 不用的硬件能力及时关闭。

6. **启动优化**
   - 减少动态库。
   - 减少 `+load` 方法。
   - 减少启动阶段初始化任务。
   - 启动后按需加载业务模块。

7. **安装包瘦身**
   - 删除无用资源。
   - 压缩图片、音频、视频。
   - 删除无用代码。
   - 分析 LinkMap。

### 项目表达

在项目里，我一般会先用工具定位性能瓶颈，比如 Time Profiler 看 CPU 消耗，Core Animation 看渲染问题，Allocations 和 Leaks 看内存问题。定位之后，再根据具体问题优化，比如列表卡顿一般从 Cell 布局、图片解码、视图层级、离屏渲染几个方面处理；启动慢则从 dyld、runtime、main 三个阶段拆解优化。

---

# 二、内存优化

## 题目 2：你在项目中是怎么优化内存的？

### 答案

内存优化主要从对象、图片、缓存、循环引用、数据结构几个方面入手。

### 1. 减少不必要对象创建

比如在列表滚动中，如果每次刷新 Cell 都创建大量对象，就会造成内存上涨和 CPU 消耗。

优化方式：

- Cell 复用。
- 数据模型提前处理。
- 避免在 `cellForRowAtIndexPath:` 中做复杂对象创建。
- 不需要事件响应的视图，可以考虑用 `CALayer` 替代 `UIView`。

`UIView` 比 `CALayer` 更重，因为 `UIView` 需要处理事件响应、布局、手势等能力。如果只是展示一条线、背景、简单图层，`CALayer` 更轻量。

---

### 2. 图片内存优化

图片是 App 内存占用的大头。

优化方式：

- 图片尺寸尽量和 `UIImageView` 显示尺寸一致。
- 大图不要直接加载原图。
- 使用缩略图。
- 图片解码放到子线程。
- 列表图片异步加载。
- 使用缓存时控制缓存大小。
- 内存警告时清理缓存。

例如一个 3000 × 3000 的图片，如果直接加载到内存，即使显示成 100 × 100，也会占用很大内存。

图片内存大致计算方式：

```objc
内存大小 = 宽 × 高 × 每个像素字节数
```

RGBA 图片通常每个像素 4 字节。

例如：

```objc
3000 × 3000 × 4 = 36,000,000 字节 ≈ 34.3 MB
```

所以图片尺寸不匹配会造成巨大浪费。

---

### 3. 避免循环引用

常见循环引用场景：

- Block 持有 self。
- Timer 持有 target。
- Delegate 使用 strong。
- CADisplayLink 持有 target。
- Notification 没有移除。
- 单例持有业务对象。

Block 中常见写法：

```objc
__weak typeof(self) weakSelf = self;
self.block = ^{
    __strong typeof(weakSelf) self = weakSelf;
    if (!self) return;

    [self doSomething];
};
```

Timer 可以通过以下方式避免循环引用：

- 使用 block timer 时 weak self。
- 使用中间代理对象。
- 页面销毁时 invalidate。

---

### 4. 合理使用缓存

缓存不能无限增长。

优化方式：

- 使用 `NSCache`。
- 设置数量限制。
- 设置内存限制。
- 收到内存警告时清理。
- 区分内存缓存和磁盘缓存。

`NSCache` 相比 `NSMutableDictionary` 的优点：

- 线程安全。
- 系统内存紧张时会自动清理部分对象。
- 可以设置数量和成本限制。

---

### 5. 大数据处理优化

如果有大量数据，不要一次性全部加载到内存。

优化方式：

- 分页加载。
- 懒加载。
- 数据库按需查询。
- 大文件流式读取。
- 避免一次性把大文件读入内存。

---

## 题目 3：为什么图片尺寸最好和 UIImageView 的 size 保持一致？

### 答案

如果图片尺寸远大于 `UIImageView` 的显示尺寸，系统在显示时需要做缩放处理，会增加 CPU/GPU 消耗，同时原图解码后会占用更大的内存。

例如：

- 图片实际尺寸：3000 × 3000
- 显示尺寸：100 × 100

如果直接加载原图，内存按照原始像素计算，而不是按照显示尺寸计算。这样会造成内存浪费，也会增加图片解码和渲染成本。

### 优化方式

- 服务端返回合适尺寸的图片。
- 本地生成缩略图。
- 使用图片库的缩略图能力。
- 列表中不要直接加载高清原图。

---

# 三、CPU 与 GPU

## 题目 4：CPU 和 GPU 在 iOS 屏幕渲染中分别负责什么？

### 答案

在 iOS 屏幕成像过程中，CPU 和 GPU 分工不同。

### CPU 主要负责

- 对象创建和销毁。
- 对象属性调整。
- 布局计算。
- 文本尺寸计算。
- 文本排版。
- 图片格式转换。
- 图片解码。
- Core Graphics 图像绘制。
- 提交渲染数据给 GPU。

### GPU 主要负责

- 图层合成。
- 纹理渲染。
- 图像混合。
- 透明度处理。
- 将渲染结果写入帧缓存。

### 总结

CPU 更偏向“计算和准备数据”，GPU 更偏向“渲染和显示”。

如果 CPU 任务太重，会导致数据来不及提交给 GPU。  
如果 GPU 任务太重，会导致渲染结果来不及显示。  
两者任何一方耗时过长，都可能造成卡顿。

---

## 题目 5：为什么 CPU 或 GPU 负载过高会导致卡顿？

### 答案

iOS 屏幕通常以 60 FPS 刷新，意味着每一帧大约只有 16.67ms 的时间。

```text
1 秒 / 60 帧 ≈ 16.67ms
```

在这 16.67ms 内，CPU 要完成布局、计算、解码、绘制准备等工作，GPU 要完成图层合成和渲染。如果任意一方超过这个时间，当前帧就无法按时显示，用户就会感受到卡顿。

### 举例

如果一帧中：

- CPU 花了 12ms。
- GPU 花了 8ms。

总耗时可能超过 16.67ms，就可能掉帧。

---

# 四、屏幕成像原理

## 题目 6：iOS 屏幕成像的大致流程是什么？

### 答案

iOS 屏幕成像大致流程：

1. CPU 计算布局、处理图片、文本排版、准备渲染数据。
2. CPU 将渲染任务提交给 GPU。
3. GPU 进行纹理渲染和图层合成。
4. 渲染结果写入帧缓存。
5. 视频控制器读取帧缓存。
6. 屏幕显示图像。

### 双缓冲机制

iOS 中通常存在前帧缓存和后帧缓存：

- **前帧缓存**：当前屏幕正在显示的内容。
- **后帧缓存**：GPU 正在渲染下一帧内容。

当下一帧渲染完成后，前后缓冲区交换，屏幕显示新的一帧。

---

## 题目 7：什么是 VSync？它和卡顿有什么关系？

### 答案

`VSync` 是垂直同步信号。屏幕刷新时，系统会按照固定频率发送 VSync 信号。

在 60Hz 屏幕上，大约每 16.67ms 会有一次 VSync 信号。

如果在下一次 VSync 到来之前，CPU 和 GPU 没有准备好下一帧画面，那么这次刷新就只能继续显示旧画面，用户看到的就是掉帧或者卡顿。

### 总结

卡顿的本质就是：

```text
下一帧没有在 VSync 到来之前准备好
```

---

# 五、卡顿产生原因

## 题目 8：造成 App 卡顿的常见原因有哪些？

### 答案

常见原因可以分为 CPU、GPU、主线程阻塞和资源加载几个方面。

### 1. CPU 方面

- 主线程做大量计算。
- 主线程 JSON 解析。
- 主线程图片解码。
- 主线程文本计算。
- 主线程数据库操作。
- 频繁布局计算。
- Autolayout 约束复杂。
- 对象频繁创建和销毁。

### 2. GPU 方面

- 视图层级太深。
- 同屏图层太多。
- 大量透明图层。
- 大量圆角、阴影、遮罩。
- 离屏渲染。
- 图片尺寸过大。
- 短时间显示大量图片。

### 3. I/O 方面

- 主线程读写文件。
- 主线程读写数据库。
- 频繁写入小文件。
- 启动阶段加载大量本地资源。

### 4. 网络方面

- 主线程等待网络结果。
- 网络回调中做大量数据处理。
- 大量请求同时回来后集中刷新 UI。

### 5. 业务逻辑方面

- 页面初始化时加载太多模块。
- 列表 Cell 中做复杂计算。
- 每次滚动都重新计算高度。
- 重复刷新 UI。

---

# 六、列表卡顿优化

## 题目 9：列表卡顿的原因可能有哪些？

### 答案

列表卡顿通常发生在快速滚动时，原因包括：

1. Cell 高度计算复杂。
2. Cell 内部视图层级太深。
3. Cell 使用复杂 Autolayout。
4. 图片在主线程解码。
5. 图片尺寸过大。
6. Cell 中存在圆角、阴影、透明图层。
7. `cellForRowAtIndexPath:` 中做了耗时操作。
8. 数据格式转换放在主线程。
9. 文本高度每次重复计算。
10. 网络图片没有缓存。
11. 滚动过程中频繁刷新列表。
12. 主线程同时执行其他任务。

---

## 题目 10：你平时怎么优化 UITableView 或 UICollectionView 卡顿？

### 答案

### 1. Cell 复用

确保使用复用机制：

```objc
UITableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:@"Cell" forIndexPath:indexPath];
```

避免每次都创建新 Cell。

---

### 2. 提前计算 Cell 高度

不要在滚动过程中重复计算高度。

可以在数据模型中提前缓存高度：

```objc
@interface FeedModel : NSObject

@property (nonatomic, assign) CGFloat cellHeight;

@end
```

加载数据时提前计算：

```objc
model.cellHeight = [self calculateHeightWithModel:model];
```

然后直接返回：

```objc
- (CGFloat)tableView:(UITableView *)tableView heightForRowAtIndexPath:(NSIndexPath *)indexPath {
    FeedModel *model = self.dataArray[indexPath.row];
    return model.cellHeight;
}
```

---

### 3. 减少 Cell 视图层级

视图层级越深，布局和渲染成本越高。

优化方式：

- 删除无用容器 View。
- 能合并的背景图合并。
- 简单分割线用 `CALayer`。
- 固定样式可以考虑异步绘制成一张图。

---

### 4. 避免主线程图片解码

图片首次显示时可能发生解码，如果在主线程进行，会造成卡顿。

优化方式：

- 子线程提前解码。
- 使用成熟图片库。
- 滚动停止后再加载高清图。
- 列表中优先显示缩略图。

---

### 5. 图片尺寸优化

列表中不要加载过大的原图。

优化方式：

- 服务端返回缩略图。
- 本地裁剪缩放。
- 图片尺寸接近显示尺寸。

---

### 6. 减少离屏渲染

Cell 中常见离屏渲染场景：

- 头像圆角 + `masksToBounds = YES`
- 阴影
- mask
- 光栅化

优化方式：

- 头像由服务端返回圆角图。
- 本地提前绘制圆角图。
- 阴影设置 `shadowPath`。
- 避免不必要的 mask。

---

### 7. 减少透明图层

如果 View 不透明，设置：

```objc
view.opaque = YES;
```

背景色不要使用透明色。

---

### 8. 控制刷新频率

不要频繁调用：

```objc
[tableView reloadData];
```

能局部刷新就局部刷新：

```objc
[tableView reloadRowsAtIndexPaths:@[indexPath] withRowAnimation:UITableViewRowAnimationNone];
```

数据变化较多时可以批量更新，避免连续多次刷新。

---

## 题目 11：为什么不建议在 cellForRowAtIndexPath 中做耗时操作？

### 答案

`cellForRowAtIndexPath:` 会在列表滚动过程中频繁调用。如果在里面做耗时操作，会直接阻塞主线程，导致滚动掉帧。

不建议在里面做：

- 网络请求。
- 图片同步加载。
- 图片解码。
- 大量文本计算。
- JSON 解析。
- 数据库查询。
- 复杂日期格式化。
- 大量对象创建。

### 推荐做法

在数据准备阶段提前处理，Cell 只负责展示。

---

# 七、离屏渲染

## 题目 12：什么是离屏渲染？

### 答案

离屏渲染是指 GPU 不直接在当前屏幕缓冲区进行渲染，而是在屏幕缓冲区之外新开一个缓冲区进行渲染。渲染完成后，再把离屏缓冲区的结果合成到当前屏幕上。

### 两种渲染方式

1. **On-Screen Rendering**
   - 当前屏幕渲染。
   - 直接在当前用于显示的屏幕缓冲区进行渲染。

2. **Off-Screen Rendering**
   - 离屏渲染。
   - 在当前屏幕缓冲区以外新建缓冲区进行渲染。

---

## 题目 13：为什么离屏渲染会影响性能？

### 答案

离屏渲染影响性能的原因主要有两个：

### 1. 创建新的缓冲区

GPU 需要额外创建一个离屏缓冲区，这会增加内存和渲染成本。

### 2. 上下文切换

GPU 需要在当前屏幕缓冲区和离屏缓冲区之间切换。

流程类似：

```text
当前屏幕缓冲区
    ↓
切换到离屏缓冲区
    ↓
完成离屏渲染
    ↓
切回当前屏幕缓冲区
    ↓
合成显示
```

这种切换会增加渲染耗时，尤其是在列表中大量 Cell 同时触发离屏渲染时，容易造成卡顿。

---

## 题目 14：哪些操作会触发离屏渲染？

### 答案

常见触发离屏渲染的操作有：

1. 光栅化

```objc
layer.shouldRasterize = YES;
```

2. 遮罩

```objc
layer.mask = maskLayer;
```

3. 圆角 + 裁剪

```objc
layer.cornerRadius = 10;
layer.masksToBounds = YES;
```

4. 阴影

```objc
layer.shadowColor = [UIColor blackColor].CGColor;
layer.shadowOpacity = 0.3;
layer.shadowRadius = 5;
layer.shadowOffset = CGSizeMake(0, 2);
```

如果设置了 `shadowPath`，通常可以避免阴影带来的离屏渲染：

```objc
view.layer.shadowPath = [UIBezierPath bezierPathWithRect:view.bounds].CGPath;
```

---

## 题目 15：圆角一定会触发离屏渲染吗？

### 答案

不一定。

通常来说，单独设置 `cornerRadius` 不一定触发明显的离屏渲染。  
但如果同时设置：

```objc
layer.cornerRadius > 0
layer.masksToBounds = YES
```

就容易触发离屏渲染。

尤其是在图片、复杂图层、列表 Cell 中，这种写法会增加 GPU 压力。

### 优化方式

1. 使用 CoreGraphics 提前绘制圆角图片。
2. 让设计提供圆角图片。
3. 使用 CAShapeLayer 做圆角遮罩时要注意成本。
4. 列表头像可以提前生成圆角缓存。
5. 如果只是背景圆角，可以考虑不裁剪子视图。

---

## 题目 16：阴影为什么容易造成性能问题？怎么优化？

### 答案

阴影的渲染成本高，是因为系统需要动态计算阴影区域。如果没有指定 `shadowPath`，系统不知道阴影形状，只能根据图层内容实时计算，这会增加渲染成本，甚至触发离屏渲染。

### 优化方式

给阴影设置明确路径：

```objc
view.layer.shadowColor = [UIColor blackColor].CGColor;
view.layer.shadowOpacity = 0.2;
view.layer.shadowRadius = 6;
view.layer.shadowOffset = CGSizeMake(0, 2);
view.layer.shadowPath = [UIBezierPath bezierPathWithRoundedRect:view.bounds cornerRadius:8].CGPath;
```

这样 GPU 可以直接按照指定路径绘制阴影，减少动态计算。

---

# 八、卡顿检测

## 题目 17：你是怎么检测 App 卡顿的？

### 答案

常见检测方式有：

1. Instruments Time Profiler。
2. Core Animation。
3. FPS 监控。
4. 主线程 RunLoop 监控。
5. 线上卡顿监控 SDK。

其中 RunLoop 监控是一种常见线上卡顿检测方案。

---

## 题目 18：如何通过 RunLoop 监控卡顿？

### 答案

主线程的任务执行依赖 RunLoop。平时所说的“卡顿”，通常是主线程执行某个任务时间过长，导致 RunLoop 长时间停留在某个状态。

可以给主线程 RunLoop 添加 Observer，监听状态变化：

- `kCFRunLoopBeforeSources`
- `kCFRunLoopBeforeWaiting`
- `kCFRunLoopAfterWaiting`

如果 RunLoop 在某个状态停留时间过长，就可以认为发生了卡顿。

### 原理

正常情况下，RunLoop 会不断切换状态。如果主线程被某个任务阻塞，RunLoop 状态就长时间不变化。监控线程可以通过信号量或时间差判断是否超过阈值。

### 简化思路

```text
主线程 RunLoop 状态变化
        ↓
Observer 记录状态
        ↓
子线程定时检测
        ↓
超过阈值仍未变化
        ↓
认为主线程卡顿
        ↓
采集主线程调用栈
```

### 追问：为什么要采集主线程调用栈？

因为只知道卡顿还不够，需要知道卡顿发生在哪段代码。采集调用栈可以帮助定位具体耗时方法。

---

# 九、耗电优化

## 题目 19：App 耗电主要来源有哪些？

### 答案

主要来源有：

1. CPU 处理。
2. 网络请求。
3. 定位服务。
4. 图像处理。
5. 传感器硬件。
6. 后台任务。
7. 高频定时器。

---

## 题目 20：如何做耗电优化？

### 答案

耗电优化核心是减少 CPU、GPU、网络、定位和硬件传感器的使用。

---

### 1. 降低 CPU/GPU 功耗

- 减少主线程计算。
- 减少频繁 UI 刷新。
- 减少复杂动画。
- 避免离屏渲染。
- 降低图片处理成本。

---

### 2. 少用定时器

定时器会频繁唤醒 CPU。

优化方式：

- 不需要时及时 invalidate。
- 降低触发频率。
- 页面不可见时暂停。
- 可以合并多个定时任务。
- 使用系统更合适的后台机制。

---

### 3. I/O 优化

频繁读写磁盘会增加耗电。

优化方式：

- 不要频繁写入小数据。
- 小数据尽量合并后批量写入。
- 大量数据使用数据库。
- 大文件读写考虑使用 `dispatch_io`。

`dispatch_io` 是基于 GCD 的异步文件 I/O API，系统可以优化磁盘访问。

---

### 4. 网络优化

网络是耗电大户。

优化方式：

1. 减少网络请求次数。
2. 压缩请求和响应数据。
3. 对相同请求结果使用缓存。
4. 使用断点续传，避免网络中断后重复下载。
5. 网络不可用时不要反复请求。
6. 设置合适的超时时间。
7. 支持用户取消慢请求。
8. 批量传输数据。

例如下载视频流或广告资源时，不要频繁下载很小的数据包，可以一大块一大块下载。

---

### 5. 定位优化

定位会使用 GPS、Wi-Fi、基站等能力，耗电明显。

优化方式：

1. 只需要快速定位时，使用：

```objc
[locationManager requestLocation];
```

定位完成后，系统会自动让定位硬件断电。

2. 非导航类 App 不要持续定位。

3. 定位完成后及时停止：

```objc
[locationManager stopUpdatingLocation];
```

4. 降低定位精度：

```objc
locationManager.desiredAccuracy = kCLLocationAccuracyHundredMeters;
```

不要随便使用：

```objc
kCLLocationAccuracyBest
```

5. 后台定位时设置：

```objc
locationManager.pausesLocationUpdatesAutomatically = YES;
```

这样当系统判断用户不太可能移动时，会自动暂停位置更新。

---

### 6. 硬件检测优化

加速度计、陀螺仪、磁力计等传感器也会耗电。

优化方式：

- 不需要时及时关闭。
- 页面不可见时停止。
- 降低采样频率。
- 不要长时间后台采集。

---

# 十、App 启动优化

## 题目 21：App 启动分为哪几种？

### 答案

App 启动分为两种：

1. **冷启动 Cold Launch**
   - App 从零开始启动。
   - App 进程不存在，系统需要重新加载可执行文件、动态库、初始化 runtime、执行 main 函数。

2. **热启动 Warm Launch**
   - App 已经在内存中。
   - 从后台回到前台。
   - 速度通常比冷启动快。

启动优化主要针对冷启动。

---

## 题目 22：App 冷启动分为哪几个阶段？

### 答案

冷启动可以分为三个阶段：

1. dyld 阶段。
2. runtime 阶段。
3. main 阶段。

---

## 题目 23：dyld 阶段做了什么？

### 答案

`dyld` 是 Apple 的动态链接器，全称 dynamic link editor。

启动 App 时，dyld 主要做：

1. 加载 App 可执行文件。
2. 加载依赖的动态库。
3. 递归加载动态库依赖的其他动态库。
4. 进行符号绑定。
5. 进行重定位。
6. 加载完成后通知 Objective-C runtime。

### 简单理解

dyld 阶段就是把可执行文件和依赖库加载到内存，为后续 runtime 初始化做准备。

---

## 题目 24：runtime 阶段做了什么？

### 答案

runtime 阶段主要做 Objective-C 相关结构的初始化。

包括：

1. 调用 `map_images` 解析 Mach-O 中的 Objective-C 信息。
2. 注册 Objective-C 类。
3. 初始化类对象。
4. 注册分类。
5. 注册协议。
6. 注册 selector。
7. 调用所有 Class 和 Category 的 `+load` 方法。
8. 调用 C++ 静态初始化器。
9. 调用 `__attribute__((constructor))` 修饰的函数。

到这个阶段结束时，可执行文件和动态库中的类、协议、方法、IMP 等符号都已经被 runtime 管理起来。

---

## 题目 25：main 阶段做了什么？

### 答案

当前面 dyld 和 runtime 初始化完成后，dyld 会调用 `main` 函数。

`main` 函数中通常会调用：

```objc
UIApplicationMain(argc, argv, nil, NSStringFromClass([AppDelegate class]));
```

然后进入：

```objc
application:didFinishLaunchingWithOptions:
```

在这个阶段，App 会做业务初始化，比如：

- 创建窗口。
- 设置根控制器。
- 初始化 SDK。
- 读取本地配置。
- 注册推送。
- 初始化日志系统。
- 拉取启动配置。

如果这些任务全部放在启动阶段同步执行，就会导致启动变慢。

---

## 题目 26：如何查看 App 启动时间？

### 答案

可以通过 Xcode 添加环境变量查看启动耗时。

路径：

```text
Edit Scheme -> Run -> Arguments -> Environment Variables
```

添加：

```text
DYLD_PRINT_STATISTICS = 1
```

如果需要更详细的信息，可以添加：

```text
DYLD_PRINT_STATISTICS_DETAILS = 1
```

这样控制台会打印 dyld 阶段的耗时分析。

---

## 题目 27：如何优化 dyld 阶段？

### 答案

dyld 阶段优化主要是减少加载内容。

### 1. 减少动态库数量

动态库越多，dyld 加载和链接成本越高。

优化方式：

- 清理无用动态库。
- 合并部分动态库。
- 能静态链接的库考虑静态化。
- 定期检查依赖。

---

### 2. 减少 Objective-C 类、分类、Selector 数量

类、分类、Selector 越多，runtime 需要处理的信息越多。

优化方式：

- 删除无用类。
- 删除无用分类。
- 清理无用方法。
- 避免无意义的工具分类泛滥。

---

### 3. 减少 C++ 虚函数数量

C++ 虚函数会增加符号处理成本，过多时会影响启动。

---

### 4. Swift 尽量使用 struct

在合适场景下，Swift 使用 `struct` 可以减少 Objective-C runtime 参与度。

---

## 题目 28：如何优化 runtime 阶段？

### 答案

runtime 阶段优化重点是减少启动时自动执行的代码。

### 1. 少用 `+load`

`+load` 会在 runtime 加载类和分类时自动调用，而且调用时机非常早。大量 `+load` 会拖慢启动。

优化方式：

- 用 `+initialize` 替代部分 `+load`。
- 用 `dispatch_once` 做懒加载。
- 把不必要的初始化延迟到真正使用时。

---

### 2. 少用 C++ 静态构造器

C++ 静态对象的构造函数会在启动阶段执行，影响启动速度。

---

### 3. 少用 `__attribute__((constructor))`

这种函数也会在 main 函数之前执行，过多会拖慢启动。

---

### 4. 初始化任务懒加载

不是启动必须的内容，不要放在启动阶段执行。

---

## 题目 29：如何优化 main 阶段？

### 答案

main 阶段优化主要集中在 `application:didFinishLaunchingWithOptions:`。

优化方式：

1. 减少同步任务。
2. 延迟非必要 SDK 初始化。
3. 首页只加载首屏必要数据。
4. 非首屏模块按需加载。
5. 避免启动阶段大量读写磁盘。
6. 避免启动阶段大量网络请求。
7. 启动任务分级：
   - 必须立即执行。
   - 首屏前执行。
   - 首屏后执行。
   - 空闲时执行。
8. 使用异步初始化，但注意线程安全。

### 项目表达

我会把启动任务拆分成不同优先级。比如日志、崩溃采集、基础配置属于启动必要任务；埋点、广告、非首页业务 SDK 可以延迟到首屏展示之后；一些低优先级任务可以放到 RunLoop 空闲时执行。

---

# 十一、安装包瘦身

## 题目 30：iOS 安装包主要由哪些部分组成？

### 答案

IPA 安装包主要由两部分组成：

1. 可执行文件。
2. 资源文件。

资源文件包括：

- 图片。
- 音频。
- 视频。
- JSON。
- plist。
- xib。
- storyboard。
- bundle。

可执行文件包括：

- 主工程代码编译产物。
- 静态库。
- 动态库。
- 第三方 SDK。
- 符号和字符串等内容。

---

## 题目 31：资源文件如何瘦身？

### 答案

资源瘦身主要有：

1. 图片无损压缩。
2. 音频、视频压缩。
3. 删除无用图片。
4. 删除无用 bundle。
5. 删除重复资源。
6. 使用矢量图或字体图标替代部分图片。
7. 按需下载非必要资源。
8. 不同分辨率资源合理配置。
9. 检查 2x、3x 图片是否冗余。

### 工具

可以使用 `LSUnusedResources` 检查未使用资源。

---

## 题目 32：可执行文件如何瘦身？

### 答案

### 1. 编译器优化

常见配置：

```text
Strip Linked Product = YES
Make Strings Read-Only = YES
Symbols Hidden by Default = YES
```

这些配置可以减少符号、字符串等带来的体积。

---

### 2. 关闭异常支持

如果项目没有使用异常机制，可以关闭：

```text
Enable C++ Exceptions = NO
Enable Objective-C Exceptions = NO
```

并在 `Other C Flags` 中添加：

```text
-fno-exceptions
```

---

### 3. 删除无用代码

方式：

- 使用 AppCode 的 Inspect Code。
- 使用静态分析工具。
- 自定义 LLVM 插件检测未调用代码。
- 定期清理废弃业务模块。
- 删除无用分类和工具方法。

---

### 4. 清理第三方 SDK

很多安装包变大是因为第三方库过多。

优化方式：

- 删除不用的 SDK。
- 替换体积过大的 SDK。
- 只接入需要的功能模块。
- 避免重复引入相同功能库。

---

# 十二、LinkMap

## 题目 33：什么是 LinkMap？

### 答案

LinkMap 是链接器生成的映射文件，它记录了可执行文件的组成情况。

通过 LinkMap 可以看到：

- 每个目标文件占用大小。
- 每个类、方法、符号的大致大小。
- 第三方库占用体积。
- 哪些模块导致可执行文件变大。

---

## 题目 34：LinkMap 有什么作用？

### 答案

LinkMap 主要用于分析安装包中可执行文件的体积来源。

可以帮助我们回答：

1. 哪个库最大？
2. 哪个业务模块最大？
3. 哪些代码可能需要清理？
4. 安装包变大是哪个版本引起的？
5. 第三方 SDK 占用了多少体积？

---

## 题目 35：如何生成 LinkMap？

### 答案

在 Xcode 中打开：

```text
Build Settings
```

搜索：

```text
Write Link Map File
```

设置为：

```text
YES
```

编译后会生成 `.linkmap` 文件。

也可以借助第三方工具解析 LinkMap，例如：

```text
LinkMap 分析工具
```

---

# 十三、项目实战回答模板

## 题目 36：如果问你“项目里怎么做性能优化”，怎么回答比较完整？

### 回答模板

在项目中，我一般会先定位问题，再针对具体瓶颈优化，不会盲目优化。

首先，我会用 Instruments 看 CPU、内存和渲染问题。如果是列表卡顿，我会重点检查 Cell 高度计算、图片解码、视图层级、离屏渲染和主线程耗时任务。

比如列表页面中，我会提前计算并缓存 Cell 高度，图片使用异步加载和缓存，避免在 `cellForRowAtIndexPath:` 中做网络请求、图片解码和复杂文本计算。对于头像圆角，我会尽量使用提前绘制好的圆角图，或者缓存圆角处理结果，避免滚动过程中大量触发离屏渲染。对于阴影，会设置 `shadowPath`，减少系统动态计算阴影区域的成本。

如果是启动慢，我会按 dyld、runtime、main 三个阶段拆解。dyld 阶段减少动态库和无用类；runtime 阶段减少 `+load`、C++ 静态构造器和 constructor；main 阶段把非必要初始化延迟到首屏之后或者空闲时执行。

如果是耗电问题，我会减少定时器、优化网络请求、降低定位精度，不需要定位或传感器时及时关闭。

整体思路就是：先通过工具定位，再从 CPU、GPU、内存、I/O、网络、启动流程几个维度分别优化。

---

## 题目 37：如果问你“一个列表页面很卡，你怎么排查”，怎么回答？

### 回答模板

我会先确认卡顿发生的场景，是首次进入卡、快速滚动卡，还是数据刷新时卡。

如果是快速滚动卡，我会重点排查：

1. `cellForRowAtIndexPath:` 是否做了耗时操作。
2. Cell 高度是否每次重复计算。
3. 图片是否在主线程解码。
4. 图片尺寸是否过大。
5. Cell 层级是否过深。
6. 是否有大量圆角、阴影、mask。
7. 是否有透明图层导致 GPU 混合成本高。
8. 是否频繁调用 `reloadData`。

然后我会用 Time Profiler 看主线程耗时方法，用 Core Animation 看 FPS、Color Blended Layers、Color Offscreen-Rendered 等指标。

优化上，我会缓存高度、异步加载和解码图片、减少视图层级、减少透明图层、避免离屏渲染、局部刷新列表，并把数据处理放到子线程。

---

## 题目 38：如果问你“怎么优化启动速度”，怎么回答？

### 回答模板

启动优化我会按冷启动流程拆成 dyld、runtime、main 三个阶段。

dyld 阶段主要减少动态库数量，清理无用库，减少类、分类、Selector 数量。runtime 阶段主要减少 `+load` 方法、C++ 静态构造器和 `constructor` 函数，把启动时自动执行的逻辑改成懒加载。main 阶段主要优化 `didFinishLaunchingWithOptions`，把任务分级，首屏必须的任务同步完成，非首屏任务延后执行，低优先级任务放到首屏之后或空闲时执行。

同时可以通过 `DYLD_PRINT_STATISTICS` 和 Instruments 来分析启动耗时。

---

# 十四、高频追问汇总

## 题目 39：Autolayout 为什么比 frame 更耗性能？

### 答案

Autolayout 需要根据约束关系计算最终布局，本质上需要解约束系统。如果视图层级复杂、约束数量多，布局计算成本就会增加。

而直接设置 frame 是明确告诉系统视图的位置和大小，不需要额外推导。

所以在复杂列表中，如果 Cell 样式固定，直接 frame 布局通常性能更好。

---

## 题目 40：为什么要控制线程最大并发数？

### 答案

线程并不是越多越好。线程过多会带来：

1. 上下文切换成本。
2. 内存占用增加。
3. CPU 调度压力增加。
4. 多个任务竞争资源。
5. 可能反而降低性能。

所以对于图片解码、网络回调数据处理等任务，要合理控制并发数量。

可以使用：

```objc
NSOperationQueue *queue = [[NSOperationQueue alloc] init];
queue.maxConcurrentOperationCount = 3;
```

---

## 题目 41：shouldRasterize 一定能优化性能吗？

### 答案

不一定。

`shouldRasterize = YES` 会把图层光栅化成 bitmap 缓存起来。如果图层内容不经常变化，可能提升性能。

但是如果图层内容频繁变化，比如列表 Cell 滚动、动画过程中不断变化，那么每次都要重新光栅化，反而更耗性能。

所以它适合内容稳定、复用率高的场景，不适合频繁变化的场景。

---

## 题目 42：opaque 设置为 YES 有什么用？

### 答案

如果一个 View 是不透明的，设置：

```objc
view.opaque = YES;
```

可以告诉系统不需要处理后面图层的混合，减少 GPU 的 blended layer 成本。

如果 View 实际上有透明区域，却设置 `opaque = YES`，可能会显示异常。

---

## 题目 43：为什么透明视图会影响性能？

### 答案

透明视图需要 GPU 做图层混合。GPU 要计算当前图层颜色和下面图层颜色叠加后的最终颜色。

如果页面中有大量透明图层，就会增加 GPU 混合成本，导致渲染变慢。

优化方式：

- 不透明视图设置 `opaque = YES`。
- 背景色使用不透明颜色。
- 减少 alpha 小于 1 的视图。
- 减少重叠透明区域。

---

## 题目 44：为什么大量小网络包会耗电？

### 答案

移动设备的网络模块在发送和接收数据时需要唤醒硬件。大量小包会频繁唤醒网络模块，导致耗电增加。

相比频繁传输小包，批量传输可以减少硬件唤醒次数。

所以下载资源、同步数据时，能合并就合并，能批量就批量。

---

## 题目 45：为什么定位要降低精度？

### 答案

定位精度越高，系统可能需要使用 GPS 等更耗电的硬件。对于很多业务场景，比如城市级定位、附近推荐，并不需要最高精度。

所以应该根据业务设置合适精度，不要默认使用：

```objc
kCLLocationAccuracyBest
```

可以使用：

```objc
kCLLocationAccuracyHundredMeters
kCLLocationAccuracyKilometer
```

这样可以降低耗电。

---

# 十五、总结

iOS 性能优化的核心不是死记某个技巧，而是理解系统工作机制。

可以总结为一句话：

```text
减少主线程压力，降低 CPU/GPU 消耗，控制内存增长，减少 I/O 和网络浪费，启动阶段只做必要事情。
```

实际项目中应该遵循：

1. 先定位，再优化。
2. 先解决主线程问题。
3. 列表优先处理高度、图片、层级、离屏渲染。
4. 启动优化按 dyld、runtime、main 拆解。
5. 耗电优化重点关注 CPU、网络、定位、图像和硬件传感器。
6. 安装包瘦身重点分析资源和可执行文件。
